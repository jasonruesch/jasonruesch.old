{
  "name": "emails",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/emails/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "defaultConfiguration": "production",
      "options": {
        "command": "maizzle build"
      },
      "configurations": {
        "development": {},
        "production": {
          "command": "maizzle build production && shx cp -r dist/apps/emails/* apps/web/public/emails/"
        }
      }
    },
    "serve": {
      "executor": "nx:run-commands",
      "defaultConfiguration": "development",
      "options": {
        "command": "maizzle serve"
      },
      "configurations": {
        "development": {},
        "production": {}
      }
    }
  },
  "tags": []
}
