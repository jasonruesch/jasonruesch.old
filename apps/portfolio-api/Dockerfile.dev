# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build portfolio-api`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t portfolio-api`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

COPY package*.json .

RUN npm install

COPY . .

EXPOSE 3000

# Temporary fix to have the auth library built for serving
RUN npx nx build portfolio-api

CMD ["npx", "nx", "serve", "portfolio-api"]
