{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/expenses",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "root": "apps/expenses",
        "outputPath": "dist/apps/expenses"
      },
      "configurations": {
        "development": {
          "outputPath": "apps/expenses"
        },
        "production": {}
      }
    },
    "serve": {
      "executor": "@nrwl/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "expenses:build",
        "dev": true,
        "proxyConfig": "apps/expenses/proxy.conf.json"
      },
      "configurations": {
        "development": {
          "buildTarget": "expenses:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "expenses:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nrwl/next:export",
      "options": {
        "buildTarget": "expenses:build:production"
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/apps/expenses"],
      "options": {
        "jestConfig": "apps/expenses/jest.config.ts",
        "passWithNoTests": true
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/expenses/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
